<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Properties -->
    <property name="LOG_PATH" value="/app/logs" />
    <property name="LOG_FILE" value="backend.log" />

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [reqId=%X{requestId}] [corrId=%X{correlationId}] [traceId=%X{traceId}] - %msg%n</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Rolling File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Daily rollover with size threshold -->
            <fileNamePattern>${LOG_PATH}/archived/backend-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- Max file size 10MB, keep 30 days history, total cap 3GB -->
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [reqId=%X{requestId}] [corrId=%X{correlationId}] [traceId=%X{traceId}] - %msg%n</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- Profiles logging configuration -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
        <logger name="com.cloudstorage.backend" level="DEBUG" />
    </springProfile>

    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE" />
        </root>
        <logger name="com.cloudstorage.backend" level="INFO" />
    </springProfile>

    <!-- Default logger if profile is unspecified -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>
